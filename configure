#!/bin/bash

app_version=`cat VERSION`

echo "Installing Dependencies"
go get

echo "# Generated by configure script" > config.make
echo "APP_VERSION = $app_version" >> config.make
echo "export APP_VERSION" >> config.make

